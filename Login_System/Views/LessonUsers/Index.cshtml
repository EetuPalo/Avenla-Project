@model IEnumerable<Login_System.Models.LessonUser>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Members</h2>
<hr />
<p>
    <a class="btn btn-info" asp-action="Create">Create New</a>
    <a class="btn btn-danger" asp-action="Index" asp-controller="SkillCourses">Back</a>
</p>
<div class="container-fluid p-0">
    <div class="row">
        <div class="col-sm-4 border-right border-secondary">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.MemberName)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.MemberName)
                            </td>
                            <td>
                                <a asp-action="Details" asp-controller="AppUsers" asp-route-id="@item.MemberID" asp-route-source="lesson" asp-route-sourceId="@item.LessonID">Details</a> |
                                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>


