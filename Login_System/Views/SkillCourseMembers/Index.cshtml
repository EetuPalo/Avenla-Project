@model IEnumerable<Login_System.ViewModels.SkillCourseMemberVM>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@TempData.Peek("CourseName")@Resources.CourseMembers.Index_Title</h2>

<div class="container-fluid p-0">
    <div class="row mb-4">
        <div class="col-sm">
	@if(User.IsInRole("Admin"))
	{
            <a class="btn btn-primary" asp-action="Create">@Resources.CourseMembers.Index_Create</a>
	}        
            <a class="btn btn-danger" asp-action="Index" asp-controller="SkillCourses">@Resources.CourseMembers.Index_BackCourse</a>
        </div>
    </div>
</div>
<hr />
<div class="container-fluid p-0">
    <div class="row">
        <div class="col-sm-8 border-right border-dark">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>
                            @Resources.CourseMembers.Index_User
                        </th>
                        <th>
                            @Resources.CourseMembers.Index_Status
                        </th>
                        <th>
                            @Resources.CourseMembers.Index_Completed
                        </th>
                        <th>
                            @Resources.CourseMembers.Index_Progress
                        </th>
                        <th>
                            @Resources.CourseMembers.Index_Grade
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @if (User.IsInRole("Admin"))
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.UserName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Status)
                                </td>
                                <td>
                                    @if (item.CompletionDate == DateTime.MinValue)

                                    {
                                        <p> - </p>
                                    }
                                    else
                                    {
                                        @Html.DisplayFor(modelItem => item.CompletionDate)
                                    }
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DaysCompleted) / @Html.DisplayFor(modelItem => item.CourseLength)
                                </td>
                                <td>
                                    @item.CourseGrade
                                </td>
                                <td>
                                    @if (User.IsInRole("Admin") || item.UserName == User.Identity.Name)
                                    {
                                        <a asp-action="Edit" asp-route-id="@item.Id">@Resources.CourseMembers.Index_BTN_Edit</a><span> |</span>
                                        <a asp-action="Details" asp-controller="AppUsers" asp-route-id="@item.UserID" asp-route-source="SkillCourseMembers" asp-route-sourceId="@item.CourseName">@Resources.CourseMembers.Index_BTN_Details</a> <span> |</span>
                                        <a class="text-danger" asp-action="Delete" asp-route-id="@item.Id">@Resources.CourseMembers.Index_BTN_Delete</a>
                                        @if (User.IsInRole("Admin"))
                                        {
                                            <span>| </span><a class="text-info" asp-action="Grade" asp-route-id="@item.Id">@Resources.CourseMembers.Index_Grade</a>
                                        }
                                    }
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        @foreach (var item in Model.Where(x => x.UserName == User.Identity.Name))
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.UserName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Status)
                                </td>
                                <td>
                                    @if (item.CompletionDate == DateTime.MinValue)

                                    {
                                        <p> - </p>
                                    }
                                    else
                                    {
                                        @Html.DisplayFor(modelItem => item.CompletionDate)
                                    }
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DaysCompleted) / @Html.DisplayFor(modelItem => item.CourseLength)
                                </td>
                                <td>
                                    @item.CourseGrade
                                </td>
                                <td>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
        <div class="col-sm-4">
            @if (User.IsInRole("Admin"))
            {
            <h4>
                @TempData["CourseName"] @Resources.CourseMembers.Index_SubTitle
            </h4>
            <p>
                @Resources.CourseMembers.Index_SubDescStart @TempData["CourseName"]@Resources.CourseMembers.Index_SubDescEnd
            </p>
                <p>
                    <strong>@Resources.CourseMembers.Index_Enrolled</strong> @TempData["CourseMemberCount"] <br />
                    <strong>@Resources.CourseMembers.Index_CompletedStatus</strong> @TempData["CompletedCount"] <br />
                    <strong>@Resources.CourseMembers.Index_DroppedOut</strong> @TempData["DropoutCount"] <br />
                </p>
            }
            else
            {
                <h4>
                    @TempData["CourseName"]
                </h4>
                <p>
                    @Resources.CourseMembers.Index_NotAdmin
                </p>
            }
        </div>
    </div>
</div>

